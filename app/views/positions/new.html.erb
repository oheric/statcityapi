<h2>Name a New Position</h2>
	<%= form_for @position do |f| %>

	<div class="field">
		<%= f.label :positiontype %><br>
		<%= f.text_field :positiontype %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
	<%end %>